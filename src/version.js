// The one place the version number lives. EDDN records it as
// softwareVersion, so a build that lies about its version is rude to the
// network besides - when a message turns out to be malformed, this is what
// says which build produced it.
export const VERSION = "1.0";
